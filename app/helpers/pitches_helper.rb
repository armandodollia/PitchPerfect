module PitchesHelper
  def round_1_winners
    # p "--------------------------------------"
    # @pitches = Pitch.all.where(day_id: @day.id).order(:votes)
    # @votes = @pitches.map { |pitch| pitch.votes }
    # p @votes.order(points: :desc)
  end
end
